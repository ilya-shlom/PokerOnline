CREATE TABLE  users{
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username VARCHAR(20) UNIQUE NOT NULL,
    password VARCHAR(20) TEXT NOT NULL,
};

CREATE TABLE post{
    player_id INTEGER PRIMARY KEY,
    res VARCHAR(1),
    using_cheats VARCHAR(1),
    щкщкщщкщк
};